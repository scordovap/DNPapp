<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ps.dnpapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Controller.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".Controller.GPSActivity" />
        <activity android:name=".Controller.CameraActivity" />
        <activity android:name=".Controller.UsuarioActivity" />
        <activity android:name=".Controller.RegisterActivity" />
        <activity android:name=".Controller.LoginActivity" />
        <activity android:name=".Model.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".Controller.LocationBroadcastReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>

                <!-- <action android:name="android.location.LocationManager.KEY_LOCATION_CHANGED" /> -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.location.LocationManager.KEY_LOCATION_CHANGED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".Controller.LocationService"
            android:exported="false" />
        <service
            android:name=".Controller.LocationIntentService"
            android:exported="false" />
        <service
            android:name=".Controller.LocationIntentServiceBroadcast"
            android:exported="false" />
    </application>

</manifest>